@{
    ViewData["Title"] = "Home Page";
}
@model List<Learning_App.Models.Response.ListCourses>

<main role="main">

    <!--tunahan-bas2-->
    <section class="py-5 text-center position-relative bg-video">
        <video autoplay loop muted playsinline class="w-75">
            <source src="/images/welcome-bg.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
        <div class="overlay"></div>
        <div class="row py-lg-5 text-light position-absolute w-100 top-50 start-50 translate-middle">
            <div class="col-lg-6 col-md-8 mx-auto">
                <h1 class="fw-light"><strong>Beewiser</strong></h1>
                <p class="lead text-body-secondary"><strong>Unlock Your Potential, Anytime, Anywhere</strong></p>
            </div>
        </div>
    </section>
    <!--tunahan-bit2-->

    <div class="album py-5">
        <div class="container">

            <div class="row">
                @foreach (var course in Model)
                {
                    <div class="col-md-4">
                        <div class="card mb-4 box-shadow">
                            <img class="card-img-top" src="@course.ImageUrl" alt="Course Image">
                            <div class="card-body">
                                <h5 class="card-title">@course.Title</h5>
                                <p class="card-text">@course.Description</p>
                                <div class="d-flex justify-content-between">
                                    <p class="card-text"><span class="fw-bolder">Instructor:</span> @course.Instructor.Name</p>
                                    @if (Context.Session.GetInt32("user_role") == 1)
                                    {
                                        @if(course.IsEnrolled)
                                        {
                                            <a href="@Url.Action("CourseDetailView", "Home", new { courseId = course.CourseId })" class="btn btn-outline-primary">View</a>
                                        }
                                        else
                                        {
                                            <a href="@Url.Action("EnrollCourse", "Home", new { courseId = course.CourseId })" class="btn btn-outline-success">Enroll Course</a>
                                        }
                                    }
                                    
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

</main>